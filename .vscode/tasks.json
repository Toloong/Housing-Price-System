{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run FastAPI backend",
            "type": "shell",
            "command": "uvicorn backend.main:app --reload",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
